{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/vv-portfolio",
  "framework": "angular",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}