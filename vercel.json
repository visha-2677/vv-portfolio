{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/vv-portfolio/browser",
  "framework": "angular",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}